---

extends: default

ignore: |
  .venv
  .git

yaml-files:
  - '*.yaml'
  - '*.yml'
  - '.yamllint'

rules:
  line-length: disable
  truthy:
    allowed-values: ['true', 'false', 'yes', 'no']
    check-keys: true
